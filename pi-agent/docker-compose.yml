services:
  pi-stats:
    build: .
    container_name: pi-stats
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    user: root
